# Job Harvester

Personal tool to scrape and track job postings in real time.

## Setup
```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Usage
Run the scraper:
```bash
python src/scraper.py
```
